{% extends 'base.html.twig' %}
{% block title %}{{ 'actions.create'|trans({'%entity%': 'labels.home'|trans}) }}{% endblock %}
{% block body %}
    <h1>{{ 'actions.create'|trans({'%entity%': 'labels.home'|trans}) }}</h1>

    {{ form(create_home_form) }}

    <a href="{{ path('home_list') }}">{{ 'actions.cancel'|trans({'%entity%': 'labels.home'|trans}) }}</a>
{% endblock %}
