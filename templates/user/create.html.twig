{% extends 'base.html.twig' %}
{% block title %}{{ 'actions.create'|trans({'%entity%': 'labels.user'|trans}) }}{% endblock %}
{% block body %}
    <h1>{{ 'actions.create'|trans({'%entity%': 'labels.user'|trans}) }}</h1>

    {{ form(create_user_form) }}

    <a href="{{ path('user_list') }}">{{ 'actions.cancel'|trans({'%entity%': 'labels.user'|trans}) }}</a>
{% endblock %}
